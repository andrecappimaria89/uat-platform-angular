[build]
  command = "npm install --legacy-peer-deps && npm run build:prod"
  publish = "dist/uat-platform/browser"

[build.environment]
  NODE_VERSION = "20"

[build.processing]
  skip_processing = false

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
